services:
  - type: web
    name: api
    env: docker
    dockerfilePath: ./Dockerfile

  - type: worker
    name: celery
    env: docker
    dockerfilePath: ./Dockerfile

  - type: redis
    name: redis
    
